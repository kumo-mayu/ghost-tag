# ghost-tag — 一人鬼ごっこ GPS Webゲーム

## 目的

スマートフォンを持って屋外（河川敷・空き地など広い場所）を実際に走り、GPS上の仮想の「鬼」から逃げる一人用ゲーム。
最終目標は「画面を見て鬼の位置を確認する」ゲームではなく、**音を手掛かりに、見えない鬼の気配を感じながら現実空間を走って逃げる**体験。
今回作るのは完成品ではなく、実際に屋外で遊んでゲーム性を検証するための**最小プロトタイプ**。

## 開発方針（変更しない前提）

- スマホのブラウザ上で動くローカル完結アプリ。HTML / CSS / 素のJavaScript。バックエンド・外部サーバー不要。
- 対象端末: Android（OPPO Reno10 Pro 5G）、Chrome想定。
- フレームワーク不使用。将来PWA化はあり得るが初期版では不要。
- 一人用。ネットワーク同期なし。鬼は端末内シミュレーション。
- ゲームデザイン上の未確定事項は、勝手に仕様を作り込まず「調整可能なパラメータ」または「TODO」として明示する。

## 最小ループ（初期ゴール）

ページを開く → GPS取得 → START → プレイヤーから離れた位置に鬼を生成 → 鬼が独立ループで追跡 →
GPSで現実の移動を反映 → 距離に応じて音が変化 → 捕獲距離以下でゲームオーバー → 再スタート可能。

これが屋外で成立することが最優先。地図表示、複雑なAI、空間音響、複数の鬼などは対象外。

## アーキテクチャ方針

責務を分離するが、過剰設計はしない。想定モジュール：

- **geo.js** — 緯度経度間の距離・方位計算、指定方向への座標移動などの純粋な地理計算関数群。
- **gps.js** — `navigator.geolocation.watchPosition()` のラップ。更新頻度・`accuracy`保持・GPSロスト検知。
- **oniAI.js** — 鬼の座標・速度・追跡ロジック。独立したゲームループ（`setInterval`/`requestAnimationFrame`）で更新。GPS更新周期とは非同期。
- **gameState.js** — READY / RUNNING / OUT_OF_AREA / GAMEOVER などの状態遷移、経過時間、パラメータ保持。
- **audio.js** — Web Audio APIによる距離連動の音再生（距離で頻度を変える）＋実験的なステレオ方向定位（下記参照）。
- **ui.js** — 設定画面・START/STOPボタン・RUNNING中の最小表示・デバッグ表示（隠せる）。
- **main.js** — 上記を結線するエントリポイント。

GPS更新周期とゲームシミュレーション周期は明確に分離すること（鬼のAIループはGPS更新を待たずに進行する）。

## ブラウザAPI制約（実装前提として確認済み）

- **Geolocation / Wake Lock は secure context 必須。** Android Chromeでは `https://` または `localhost` 以外（LAN上のIPへの `http://` アクセスや `file://` 直接オープン）は基本的にブロックされる。
  → **配布方法: GitHub Pages で配信する。** 静的サイト・HTTPS自動・バックエンド不要という方針に合致し、PCを立てっぱなしにする必要もない。`git`/`gh` の利用はユーザー許可済み。
- 画面OFF/バックグラウンド移行時、GPS更新・タイマー・Web Audioがブラウザ側で制限される可能性がある。初期版では画面OFF動作を必須にしない。必要なら Screen Wake Lock API で画面点灯を維持し、電池消費と誤操作防止のため画面は暗く単純にする。
- 制約により実現不可能な項目は無理に実装せず、明示的にTODO/既知の制限として記載する。

## 初期パラメータ（デフォルト値・UIから調整可能）

これらは実地テストで調整する前提の暫定値。厳密な正解はまだない。

| パラメータ | 初期値（暫定） | 備考 |
|---|---|---|
| プレイエリア半径 | 300 m | 開始地点中心の円形 |
| 鬼の初期出現距離 | 150 m | プレイヤーから離れた位置に生成 |
| 鬼の移動速度 | 2.0 m/s | 人の速歩〜ジョグ程度。要調整 |
| 捕獲判定距離 | 15 m | GPS誤差（数m〜十数m）を考慮し極端に小さくしない |
| GPS更新頻度 | `watchPosition` 標準（`enableHighAccuracy: true` はUIでON/OFF可能にする） | バッテリー消費とのトレードオフ |
| 鬼AIループ間隔 | 500 ms | GPS更新とは独立 |
| エリア外の扱い | 即ゲームオーバーにはしない。警告のみ（初期値）、フラグで切替可能にする | 後日変更前提 |
| 方向手がかり（パルス＋音色） | デフォルトON | スピーカーでも機能。下記参照 |
| 左右ステレオ定位（実験的） | デフォルトOFF | 要イヤホン。下記参照 |

## 方向情報の音への反映

距離だけでは一次元の情報しか伝わらないため、方向を音に反映する機能を2種類実装。両方とも`main.js`の`playerHeading`（後述の位置推定ロジックで求めるプレイヤーの進行方向）を基準にした鬼の相対方位から計算する。視覚障害者向け歩行支援（Microsoft Soundscape等）と音響研究（回転誘導のピッチ/テンポ符号化）を調査した上で採用。

### 位置推定（`playerHeading`の求め方）

単純に2点のGPS座標の差から方位を計算するだけでは、GPS誤差（数m〜十数m）がそのままノイズになる、静止中は方向が全く分からない、という問題がある。調査の結果、以下の優先順位で求めるように改善した（`main.js`の`updatePlayerHeading()`）。

1. **`GeolocationCoordinates.heading`（ネイティブ）を最優先**: ブラウザ標準のGeolocation APIが返す進行方向（OS側のfused location providerが算出、単純な2点差分より一般に高精度）。取得できない/静止中（`null`/`NaN`）場合は次へ。
2. **2点差分＋GPS誤差ゲート**: 移動距離が`headingMinMoveM`（既定5m）**かつ**両地点の`accuracy`の合計（ノイズフロア）を上回った場合のみ採用。GPS誤差の範囲内の「移動」をノイズとして誤検出しないため。
3. **コンパス（DeviceOrientation）フォールバック（`enableCompassFallback`、デフォルトON）**: 上記2つがどちらも使えない（＝実質静止中）場合のみ、端末のコンパス方位を使う。走っている人が持つ/腕に着けたスマホは常に揺れるため、生の値ではなく円環EMA平滑化（`compassSmoothing`係数）をかけて使用。あくまで最終フォールバックであり、移動中はGPSベースの方位が常に優先される。
   - 歩数計（Pedometer API）はブラウザに標準APIが存在しないため未実装。将来的に必要ならDeviceMotionの加速度からステップ検出を自前実装する必要がある。
   - iOS Safari（`DeviceOrientationEvent.requestPermission`が存在する場合）は許可ダイアログが必要だが、対象はAndroid Chromeのためこの分岐は基本通らない想定。

- **方向手がかり（`enableDirectionalCues`、デフォルトON、スピーカーで機能）**: `main.js`の`computeDirectionCue()`。
  - **パルス数＝大まかなセクター**: ビープ1回のバーストを、進行方向に対する鬼の相対方位で1〜4回のパルス連打に置き換える（1=前方, 2=右, 3=後方, 4=左）。距離によるビープ間隔（既存）が短くなっても、バーストがはみ出さないようパルス間隔を自動的に詰める。
  - **ローパスフィルターの明瞭さ＝整列度**: 進行方向が鬼の方角に近いほど音が明瞭（フィルターのカットオフが高い）、外れるほどこもる。Soundscapeの「ビーコンに正対するとクリアな音になる」という設計を参考にした。
  - 前後は原理的に区別できない距離ベースのみの制約はここでは解消されるが、「後方」セクター内での左右のニュアンスはフィルターの明瞭さでしか出ない（パルス数は3で固定）。
- **左右ステレオ定位（`enableStereoPan`、デフォルトOFF、実験的・要イヤホン）**: `computePan()`。`StereoPannerNode`で左右に振るシンプルな方式。スマホ本体スピーカーではステレオ分離がほぼ知覚できないため効果なし（実地テストで要確認）。方向手がかり機能と情報量が重なる部分があるため、こちらは補助的なオプション程度の位置づけ。
- **未確定事項**: パルス数・フィルター周波数レンジ・パルス間隔の実地での聞こえ方、走行中に区別できるか、既存の距離テンポ表現と混ざらないかは実地テスト待ち。コンパスフォールバックの精度（磁気干渉・キャリブレーション状態）も実地で要確認。

## 安全面（UIより優先）

- 走行中に画面注視を要求しない。主要フィードバックは音。
- GPS精度(`accuracy`)が著しく悪い場合は警告。
- プレイエリア逸脱時に明確な警告。
- GPSロスト時にわかるようにする。
- 道路等での使用を想定した設計にしない（あくまで河川敷・空き地等の安全な場所前提）。

## 初期版のスコープ外（将来検討）

- 地図UI・プレイエリアのポリゴン編集・禁止領域（※デバッグ用の位置確認地図は例外として実装済み。下記「デバッグ表示」参照。プレイ本編には地図を出さない方針は変わらない）
- 複数の鬼、探索状態、先回りAI、軌跡追跡、加速など高度なAI
- 方向定位・足音・息遣い・環境音などの空間音響
- Device Orientation / 加速度・ジャイロ・地磁気センサー活用
- PWA化・オフラインキャッシュ

## デバッグ表示

プレイヤー座標・鬼座標・距離・鬼速度・GPS accuracy などを画面上で確認できる表示を用意する。通常プレイでは非表示にできること。

**デバッグ用地図（機能確認用）**: RUNNING画面のデバッグパネル内に、プレイヤー/鬼の位置を実際に描画する地図を追加（`src/mapView.js`）。切り替え可能な2方式:
- シンプル（相対位置プロット）: `<canvas>`、外部依存なし、オフラインでも動作。開始地点を原点に、`geo.js`の距離・方位計算で正確に投影。地形・道路は出ない。
- 地図タイル（OpenStreetMap + Leaflet）: 初回選択時にCDNから遅延ロード。実際の地形が見える。要ネット接続。

いずれもデバッグパネルの一部であり、通常プレイ（RUNNING画面の主要導線）には表示されない。「鬼の位置を画面で見ない」というコア方針とは独立した検証用ツールという位置づけ。

## 決定事項ログ

- プロジェクト名: `ghost-tag`
- 配布方法: GitHub Pages（HTTPS自動、静的サイト、PC不要）
- git / gh の利用はユーザー許可済み

## 未確定事項ログ（実地テスト後に決める）

- エリア外を即ゲームオーバーにするか警告のみにするか
- 鬼速度・捕獲距離・出現距離の最適値
- GPS更新頻度と電池消費のバランス
